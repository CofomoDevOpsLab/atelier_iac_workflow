[tool.poetry]
name = "workflow"
version = "0.1.0"
description = "Atelier Pulumi - workflow de travail"
authors = ["Olivier Giroux <giroux.oli@gmail.com>"]
package-mode = false

[tool.poetry.dependencies]
python = "^3.9"
pulumi = "^3.100.0"
pulumi-azure-native = "^2.0.0"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
